spring:
  application:
    name: bills-service
  datasource:
    url: jdbc:mysql://mysql.railway.internal:3306/railway
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: mTLghtBwptIRVTlJPyHlzrzEhciugbmZ
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE}
  instance:
    preferIpAddress: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace
server:
  port: 8200
